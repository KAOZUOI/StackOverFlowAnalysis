export function buildFormData(formData, data, parentKey) {  if(data.$d){    data = moment(data).format('YYYY-MM-DD');  }  if (data && typeof data === 'object' && !(data instanceof Date) && !(data instanceof File)) {    Object.keys(data).forEach(key => {      if(data[key] instanceof File){        buildFormData(formData, data[key], parentKey ? `${parentKey}` : key);      } else {        buildFormData(formData, data[key], parentKey ? `${parentKey}[${key}]` : key);      }    });  } else {    const value = data == null ? '' : data;    formData.append(parentKey, value);  }}