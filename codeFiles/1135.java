package com.tddk.junit.service;import lombok.extern.slf4j.Slf4j;import org.junit.jupiter.api.Test;import org.junit.jupiter.api.extension.ExtendWith;import org.mockito.Spy;import org.mockito.Mock;import org.mockito.junit.jupiter.MockitoExtension;import java.util.ArrayList;import java.util.List;import static org.assertj.core.api.Assertions.*;import static org.junit.jupiter.api.Assertions.*;import static org.mockito.Mockito.*;@Slf4j@ExtendWith(MockitoExtension.class)public class StudentServiceTest {    @Spy    List<String> spyList = new ArrayList<String>();    @Test    void whenUsingTheSpyAnnotation_thenObjectIsSpied() {        spyList.add(&quot;one&quot;);        spyList.add(&quot;two&quot;);        verify(spyList).add(&quot;one&quot;);        verify(spyList).add(&quot;two&quot;);        assertThat(spyList).hasSize(2);    }}