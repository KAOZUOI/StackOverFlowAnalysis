    PcPlayer pcPlayer = new PcPlayer();    pcPlayer.setUsername(&quot;Mike&quot;);    pcPlayerRepo.save(pcPlayer);    GameScore game = new GameScore();    game.setPlayer(pcPlayer);    gameScoreRepository.save(game);    // update some game's attributes    gameScoreRepository.save(game); // Exception thrown...