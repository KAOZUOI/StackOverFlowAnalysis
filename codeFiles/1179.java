package com.example.myqueenbapp;import androidx.annotation.NonNull;import androidx.appcompat.app.AppCompatActivity;import com.example.myqueenbapp.R;import android.annotation.SuppressLint;import android.content.Intent;import android.os.Bundle;import android.util.Log;import android.view.Menu;import android.view.MenuInflater;import android.view.MenuItem;import android.view.View;import android.widget.Button;import android.widget.TextView;import android.widget.Toast;import android.widget.Toolbar;import com.google.firebase.auth.FirebaseAuth;public class HomepageActivity extends AppCompatActivity implements View.OnClickListener {    Button btLogout;    TextView tvName;    MenuItem itLogout, itAbout, itProfile;    @Override    protected void onCreate(Bundle savedInstanceState)    {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_homepage);        btLogout=findViewById(R.id.btLogout);        tvName=findViewById(R.id.tvName);        btLogout.setOnClickListener(this);    }    @Override    public boolean onCreateOptionsMenu(Menu menu)    {        getMenuInflater().inflate(R.menu.menu_homepage, menu);        return true;    }    @SuppressLint(&quot;NonConstantResourceId&quot;)    @Override    public boolean onOptionsItemSelected(@NonNull MenuItem item)    {        switch(item.getItemId())        {            case R.id.itLogout:                Toast.makeText(HomepageActivity.this, &quot;this page is yet to be here&quot;, Toast.LENGTH_LONG).show();                return true;            case R.id.itAbout:                Toast.makeText(HomepageActivity.this, &quot;This page doesn't exist yet&quot;, Toast.LENGTH_LONG).show();                return true;            case R.id.itProfile:                Toast.makeText(HomepageActivity.this, &quot;this page doesn't exist yet&quot;, Toast.LENGTH_LONG).show();                return true;            default:                return super.onOptionsItemSelected(item);        }        return super.onOptionsItemSelected(item);    }    @Override    public void onClick(View v)    {        if(v==btLogout)        {            FirebaseAuth.getInstance().signOut();            Intent moveToStart = new Intent (HomepageActivity.this, MainActivity.class);            startActivity(moveToStart);        }    }}