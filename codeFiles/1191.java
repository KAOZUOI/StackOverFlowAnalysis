public static void main (String[] args) {                // if no inputs are given set the board to the default 6x6 and default card pile        if (args.length == 0){            setRows(6);            setColumns(6);            setCardNumbers(12, 2, 3, 3);        }                // if one input then set the board to 6x6 and add special cards dependent on the value inputted        else if(args.length == 1){/*            Stuff        */}                // if two inputs then sets the board dimensions to the two values        else if(args.length == 2){/*            Stuff        */}                // if three inputs then sets the board dimensions to the two values and adds the amount of special cards that was given        else if(args.length == 3){/*            Stuff        */}                Application.launch(args);    }    