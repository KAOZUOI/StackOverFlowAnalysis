import java.util.*;import java.util.regex.*;import org.json.*;public class Main {    public static void main(String[] args) {        String st = &quot;[(0, 'test.zip'), (1, 'test.zip-segment-1'), (2, 'test-segment-2'), (3, 'test.zip-segment-3')]&quot;;        int idx = st.indexOf(&quot;[&quot;);        String fix = st.substring(idx + 1, st.lastIndexOf(&quot;]&quot;));        fix = fix.replaceAll(&quot;'&quot;, &quot;&quot;);        Pattern pattern = Pattern.compile(&quot;\\(([^,]+),\\s+([^\\)]+)\\)&quot;);        Matcher matcher = pattern.matcher(fix);        Map<String, String> map = new HashMap<>();        JSONObject jsonObject = new JSONObject();        while (matcher.find()) {            String key = matcher.group(1).trim();            String value = matcher.group(2).trim();            map.put(key, value);            jsonObject.put(key, value);        }        System.out.println(&quot;Map: &quot; + map);        System.out.println(&quot;JSONObject: &quot; + jsonObject.toString());    }}