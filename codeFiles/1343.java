  @WithSession  public Uni<Void> retrieveDataAndSaveInLocalDB() {    final Sample timer = Timer.start(meterRegistry);    return Uni.createFrom()        .deferred(() -> Uni.createFrom()            .item(dbService::connectToDB))        .onFailure()        .retry()        .withBackOff(Duration.ofSeconds(3), Duration.ofMinutes(5))        .atMost(3)        .map(this::getListOfEntities)        .flatMap(entityRepository::persist)        .call(entityRepository::flush)        .invoke(() -> timer.stop(meterRegistry.timer(&quot;timer&quot;)))        .invoke(() -> Log.info(&quot;logged info&quot;));  }