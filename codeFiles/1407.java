package datastructures.algorithms;import java.io.IOException;import java.util.ArrayDeque;import java.util.Arrays;import java.util.Comparator;import java.util.HashSet;import java.util.List;import java.util.Scanner;import java.util.Set;public class PaintBucketStringSolution {    public static void main(String[] args) throws IOException {        // maintain distinct list of points that have been visited//      Set<int[]> exploredSet = new TreeSet<>(//              Comparator.comparingInt((int[] el) -> el[1]).thenComparingInt(el -> el[0]));        Set<String> exploredSet = new HashSet<>();        int W;        int H;        int X;        int Y;        int[][] picture;        Scanner sc = new Scanner(System.in);        W = sc.nextInt();        H = sc.nextInt();        X = sc.nextInt();        Y = sc.nextInt();        picture = new int[H][W];        for (int i = 0; i < H; i++) {            for (int j = 0; j < W; j++) {                picture[i][j] = sc.nextInt();            }        }        sc.close();        int color = picture[Y][X];        ArrayDeque<List<Integer>> toExploreStack = new ArrayDeque<>();        toExploreStack.add(Arrays.asList(X, Y));        while (!toExploreStack.isEmpty()) {            List<Integer> point = toExploreStack.pop();            int px = point.get(0);            int py = point.get(1);            // String.valueOf(            String pointString = String.valueOf(px) + &quot; &quot; + String.valueOf(py);            // System.out.println(exploredSet.contains(point));            boolean execute = true;            if (exploredSet.contains(pointString)) {                execute = false;            }            if (execute) {                exploredSet.add(pointString);                if (px > 0 && picture[py][px - 1] == color) {                    toExploreStack.add(Arrays.asList(px - 1, py));                }                if (px < (W - 1) && picture[py][px + 1] == color) {                    toExploreStack.add(Arrays.asList(px + 1, py));                }                if (py > 0 && picture[py - 1][px] == color) {                    toExploreStack.add(Arrays.asList(px, py - 1));                }                if (py < (H - 1) && picture[py + 1][px] == color) {                    toExploreStack.add(Arrays.asList(px, py + 1));                }            }        }        // Sorting HashSet using List        exploredSet.stream().sorted(Comparator.comparing(x -> {            String[] s = x.split(&quot; &quot;);            return s[1] + &quot; &quot; + s[0];        })).forEach(System.out::println);    }}