Set<List<Integer>> exploredSet = new HashSet<>();...if (exploredSet.contains(Arrays.asList(px, py))) {...exploredSet.add(Arrays.asList(px, py));...Comparator<List<Integer>> comp = Comparator.comparingInt((List<Integer> x) -> x.get(1)).thenComparingInt(x -> x.get(0));exploredSet.stream()        .sorted(comp)        .map(v -> v.get(0) + &quot; &quot; + v.get(1))        .forEach(System.out::println);