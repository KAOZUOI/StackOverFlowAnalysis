public class ReadCertainLastLines {    ReadCertainLastLines() {        ArrayList<String> readLines = new ArrayList<>();        String path = &quot;path to csv file&quot;;        int noOfLinesToReadFromEnd = 10;        readAll(path, readLines);        String[] last_10 = extractLines(readLines, noOfLinesToReadFromEnd);        System.out.println(Arrays.toString(last_10));    }    public static void main(String[] args) {        new ReadCertainLastLines();    }    public void readAll(String path, ArrayList<String> toWhichArray) {        try {            String line;            BufferedReader br = new BufferedReader(new FileReader(path));            while ((line = br.readLine()) != null) {                String[] values = line.split(&quot;,&quot;);                toWhichArray.add(Arrays.toString(values));            }            br.close();        } catch (IOException e) {            e.printStackTrace();        }    }    public String[] extractLines(ArrayList<String> originalArray, int noOfLines) {        String[] cutOffList = new String[noOfLines];        for (int loop = 0; loop <= noOfLines - 1; loop++) {            cutOffList[loop] = originalArray.get(originalArray.size() - noOfLines + loop);        }        return cutOffList;    }}