public class ReadCertainLines2 {    ReadCertainLines2() {        String csvFile = &quot;your_file.csv&quot;;        int linesToKeep = 10;        String[] last_10 = ReadCertainLines(csvFile, linesToKeep);        System.out.println(Arrays.toString(last_10));    }    public static void main(String[] args) {        new ReadCertainLines2();    }    public String[] ReadCertainLines(String csvFile, int linesToKeep) {        ArrayList<String> lines = new ArrayList<>();        ArrayList<String> extractedLines = new ArrayList<>();        try (BufferedReader br = new BufferedReader(new FileReader(csvFile))) {            String line;            // Read all lines from the CSV file            while ((line = br.readLine()) != null) {                lines.add(line);            }            // Keep only the last 10 lines            int startIndex = lines.size() > linesToKeep ? lines.size() - linesToKeep : 0;            for (int i = startIndex; i < lines.size(); i++) {                extractedLines.add(lines.get(i));            }        } catch (IOException e) {            e.printStackTrace();        }        return extractedLines.toArray(new String[0]);    }}