public class ReadCertainLinesCommons {    ReadCertainLinesCommons() {        String csvFile = &quot;path to csv file&quot;;        int noOfLinesToReadFromEnd = 10;                String[] last_10 = extractLines(readLines, noOfLinesToReadFromEnd);    System.out.println(Arrays.toString(last_10));    }    public static void main(String[] args) {        new ReadCertainLinesCommons();    }    public String[] ReadLastCertainLinesWithCommons(String csvFile, int lastLinesToKeep) {        ArrayList<String> toWhichArray = new ArrayList<>(lastLinesToKeep);        try (ReversedLinesFileReader reader = new ReversedLinesFileReader(new File(csvFile), Charset.defaultCharset())) {            // Read the last 10 lines            String line;            int lineCount = 0;            while ((line = reader.readLine()) != null && lineCount <= lastLinesToKeep - 1) {                String[] values = line.split(&quot;,&quot;);                toWhichArray.add(Arrays.toString(values));                lineCount++;            }        } catch (IOException e) {            e.printStackTrace();        }        return toWhichArray.toArray(new String[0]);    }}