public class ReadLast10LinesCSV {     public static void main(String[] args) {         String csvFile = &quot;your_file.csv&quot;;        List<String> lines = new ArrayList<>();        int linesToKeep = 10;         try (BufferedReader br = new BufferedReader(new FileReader(csvFile))) {             String line;             // Read all lines from the CSV file            while ((line = br.readLine()) != null) {                lines.add(line);            }             // Keep only the last 10 lines            int startIndex = lines.size() > linesToKeep ? lines.size() - linesToKeep : 0;            for (int i = startIndex; i < lines.size(); i++) {                System.out.println(lines.get(i));            }         } catch (IOException e) {            e.printStackTrace();        }    }}