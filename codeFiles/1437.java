@Service@Transactionalpublic class BlogService {    @Lazy    @Autowired    private BlogRepository blogRepository;    public Blog saveBlogContent(MultipartFile file, Blog blogContent) throws IOException {        String blogImgURl = ServletUriComponentsBuilder.fromCurrentContextPath().path(&quot;/api/blogimage/&quot;).toUriString();                long millis = System.currentTimeMillis();        java.sql.Date date = new java.sql.Date(millis);        blogContent.setImage(file.getBytes());                blogContent.setPublishDate(date);        blogContent.setBlogImageUrl(blogImgURl);        Blog blog = blogRepository.saveAndFlush(blogContent);        return blog;    }}