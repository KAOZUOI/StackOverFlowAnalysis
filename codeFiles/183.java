Recommendations---------------* General  * Use a JDK >= 15. Changes to the networking code in 15 improved performance for TCP (and UDP datagram sockets)    a lot: http://belaban.blogspot.com/2020/07/double-your-performance-virtual-threads.html  * Use virtual threads (set use_virtual_threads=true in the transport). This reduces context switching when more    threads than cores are used.  * Remove TIME when up/down measurements are not needed anymore; every message sent/received requires a    System.nanoTime(), slowing things down a bit* TCP:  * Low latency:    * buffered_output_stream_size=0 (or a low value)    * bundler_type=&quot;no-bundler&quot;      * Investigate: use &quot;per-destination&quot; bunder_type, but also use OOB|DONT_BUNDLE  * High throughput: set buffered_output_stream to a high value (e.g. 65k)* UNICAST3  * ack_threshold: <investigate>