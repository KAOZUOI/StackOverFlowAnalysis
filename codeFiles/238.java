  JsonArrayRequest jsonArrayRequest = new JsonArrayRequest(Request.Method.GET, JSON_URL, null, new Response.Listener<JSONArray>() {        @Override        public void onResponse(JSONArray response) {            for (int i = 0; i < response.length(); i++) {                // creating a new json object and                // getting each object from our json array.                try {                    // we are getting each json object.                    JSONObject responseObj = response.getJSONObject(i);                    String word = responseObj.getString(&quot;word&quot;);                    String phonetic = responseObj.getString(&quot;phonetic&quot;);                    JSONArray jsonArray = responseObj.getJSONArray(&quot;meaning&quot;);                    for (int k = 0; k < response.length(); k++) {                      ?? what to do here ????????------------------------need help here-------------------------------                    }                    synonymsList.add(new synonymsModel(word,phonetic, synonyms,antonyms));                    buildRecyclerView();                } catch (JSONException e) {                    e.printStackTrace();                }            }        }    }, new Response.ErrorListener() {        @Override        public void onErrorResponse(VolleyError error) {            Toast.makeText(getApplicationContext(), error.getMessage(), Toast.LENGTH_LONG).show();        }    });    queue.add(jsonArrayRequest);}