(...)public class TrackFragment extends Fragment{    final int CAMERA_REQ_CODE = 100;    ImageView imgCamera;    private final Context trackFragmentContext = this.getContext();(...)        @Override    public void onViewCreated(@NonNull View view, @Nullable Bundle savedInstanceState) {        super.onViewCreated(view, savedInstanceState);        // TODO handling camera input to upload image        imgCamera = view.findViewById(R.id.imgCamera);        Button btnCamera = view.findViewById(R.id.btnCamera);        btnCamera.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                // start intent (one of these causes crash)                Intent iCamera = new Intent(MediaStore.ACTION_IMAGE_CAPTURE);                startActivityForResult(iCamera, CAMERA_REQ_CODE);            }        });    }    // show photo on track fragment below button    @Override    public void onActivityResult(int requestCode, int resultCode, @Nullable Intent data) {        super.onActivityResult(requestCode, resultCode, data);        if(resultCode==RESULT_OK){            if(requestCode==CAMERA_REQ_CODE){                Bitmap img = (Bitmap) (data.getExtras().get(&quot;data&quot;));                imgCamera.setImageBitmap(img);            }        }    }}