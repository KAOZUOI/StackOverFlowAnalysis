if (isExpressionInInventory(expression, inventoryPlayer1)) {    for (int i = 0; i < expression.length(); i++) {        char character  = expression.charAt(i);        for (int j = 0; j < inventoryPlayer1.length(); j++) {            if (inventoryPlayer1.charAt(j) == character) {                inventoryPlayer1 = inventoryPlayer1.replace(character, ' ');                expression = expression.replace(character, ' ');            }        }    }    inventoryPlayer1 = inventoryPlayer1.replaceAll(&quot;\\s+&quot;, &quot;&quot;);}