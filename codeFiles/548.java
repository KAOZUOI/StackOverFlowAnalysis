   public class CallReceiver extends BroadcastReceiver {        public CallReceiver() {        super();    }    @Override    public void onReceive(Context context, Intent intent) {        Bundle bundle;        String state;        String incomingNumber;        String action = intent.getAction();        int BLE = intent.getIntExtra(AudioManager.EXTRA_SCO_AUDIO_STATE, -1); // always return -1         if (BLE == AudioManager.SCO_AUDIO_STATE_CONNECTED){  // never connected            // start bluetooth headset            AudioManager audioManager = (AudioManager) context.getSystemService(Context.AUDIO_SERVICE)            audioManager.setMode(AudioManager.MODE_IN_CALL);            audioManager.setBluetoothScoOn(true);            audioManager.startBluetoothSco();        }    }}