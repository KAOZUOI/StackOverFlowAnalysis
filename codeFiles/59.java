    // Define the input stringString input = &quot;This is a test string with some repeating words&quot;;// Split the string into an array of wordsString[] words = input.split(&quot;\\s+&quot;);// Use the distinct method to remove duplicate words from the arrayString[] distinctWords = Arrays.stream(words).distinct().toArray(String[]::new);// Print the distinct wordsSystem.out.println(Arrays.toString(distinctWords));