try (Socket socket = new Socket(&quot;localhost&quot;, Constant.PORT)) {            for (int i = 0; i < numberOfRequestsPerClient; i++) {                List<Integer> arr = new ArrayList<>();                Random r = new Random();                for (int j = 0; j < numberOfArrayElements; j++) {                    arr.add(r.nextInt());                }                var request = ArrRequest.newBuilder()                        .setN(numberOfArrayElements)                        .addAllArray(arr)                        .build();                request.writeDelimitedTo(socket.getOutputStream());