enum DayOfWeek {    MONDAY(1), TUESDAY(2), WEDNESDAY(3), THURSDAY(4), FRIDAY(5), SATURDAY(6), SUNDAY(7);    private final int value;    private DayOfWeek(int value) {        this.value = value;    }    public int getValue() {        return value;    }    public DayOfWeek getPreviousDay() {        if (value == 1) return DayOfWeek.SUNDAY;        if (value == 2) return DayOfWeek.MONDAY;        // Insert other checks here    }    public DayOfWeek getNextDay() {        if (value == 7) return DayOfWeek.MONDAY;        // Insert other checks here    }}