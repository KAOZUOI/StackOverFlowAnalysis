        // Finisher: Return the final formatted table string        sb -> {            StringBuilder headerRow = new StringBuilder();            // Build the header in headerRow with proper column widths using keys and task names            // E.g. Student........|Phalaxing |Shieldwalling |Tercioing |Wedging |Total |Mark |            sb.insert(0, headerRow); // This inserts the header at the first position in the string builder            // You may prepare the average row also similarly, and append at the end            StringBuilder avgRow = new StringBuilder();            // Build average row here            sb.append(avgRow);            return sb.toString();        });