ALTER PROCEDURE callProcedure    (@newTable varchar(100),     @tableField varchar(8000),     @table varchar(100))ASBEGIN    DECLARE @query NVARCHAR(MAX)    SET @query = 'DELETE FROM ' + @newTable + ';';    SET @query += 'SET IDENTITY_INSERT ' + @newTable + ' ON;';    SET @query += 'INSERT INTO ' + @newTable + '(' + @tableField + ') SELECT ' + @tableField + ' FROM ' + @table + ';';    SET @query += 'SET IDENTITY_INSERT ' + @newTable + ' OFF;';    EXEC (@query)END