public class MainActivity<OneTime> extends AppCompatActivity {    private Integer OneTime = 0;    private WebView webView;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        WebView webView = findViewById(R.id.web_view);        WebSettings webSettings = webView.getSettings();        webSettings.setJavaScriptEnabled(true);        // etc.        Uri uri = this.getIntent().getData();        if (uri != null) {            url = uri.toString();        }        // To prevent reload of the first page in the webView        if (OneTime == 0) {            webView.loadUrl(url);            OneTime = 1;        }        webView.addJavascriptInterface(new JavaScriptInterface(), &quot;android&quot;);    } // End of onCreate    private class JavaScriptInterface {        @JavascriptInterface        public void mybackbutton(String message1) {             finish();         }    }} // End of MainActivity// When navigating from your first Javascript page to your next// page by means of your own button or hyperlink, you will be able // to press your own back button on that new page and return to the// first page without reloading it. I wanted it not to reload because // of the google map on it.