import java.util.Scanner;public class Main {            public static void main(String[] args) {                Scanner sc = new Scanner(System.in);                String input = sc.nextLine();                System.out.println(input);                String[] sentences = input.split(&quot;\\. &quot;);                char[] reversedText = input.toCharArray();                char[] normalizedText = input.toCharArray();                int sentenceCount = 0;                int vowelCount = 0;                int consonantCount = 0;                for (String sentence : sentences) {                    vowelCount = 0;                    consonantCount = 0;                    sentenceCount++;                    System.out.println(sentence);                    String[] words = sentence.split(&quot; &quot;);                    System.out.println(&quot;Number of words is &quot; + words.length);                    String lowerCase = sentence.toLowerCase();                    char[] chars = lowerCase.toCharArray();                    System.out.println(&quot;Number of characters is &quot; + sentence.length());                    for (char c : chars) {                        if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' || c == 'y') {                            vowelCount++;                        } else if (c == 'h' || c == 'k' || c == 'r' || c == 'd' || c == 't' || c == 'n') {                            consonantCount++;                        }                    }                    System.out.println(&quot;Number of vowels is &quot; + vowelCount);                    System.out.println(&quot;Number of consonants is &quot; + consonantCount);                }                System.out.println(&quot; &quot;);                System.out.println(&quot;Number of sentences is &quot; + sentenceCount);                for (int i = (reversedText.length) - 1; i >= 0; i--) {                    System.out.print(reversedText[i]);                }                System.out.println(&quot; &quot;);                char[] diacritics = {'ě', 'š', 'č', 'ř', 'ž', 'ý', 'á', 'í', 'é', 'ú', 'ů', 'Ě', 'Š', 'Č', 'Ř', 'Ž', 'Ý', 'Á', 'Í', 'É', 'Ú', 'Ů' };                char[] normal = {'e', 's', 'c', 'r', 'z', 'y', 'a', 'i', 'e', 'u', 'u', 'E', 'S', 'C', 'R', 'Z', 'Y', 'A', 'I', 'E', 'U', 'U' };                for (int i = 0; i < normalizedText.length; i++) {                    char c = normalizedText[i];                    int index = -1;                    for (int j = 0; j < diacritics.length; j++) {                        if (c == diacritics[j]) {                            index = j;                            break;                        }                    }                    if (index != -1) {                        normalizedText[i] = normal[index];                    }                }                System.out.println(normalizedText);            }}