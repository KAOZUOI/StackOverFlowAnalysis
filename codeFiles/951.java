private static int getNumber(String rle, int initial){    int sum = 0;    while (initial < rle.length())    {     if (Character.isDigit(rle.charAt(initial)))        {        sum = sum * 10 + Integer.parseInt(String.valueOf(rle.charAt(initial)));         initial++;        }        else        {            initial++;        }    }    return sum;}   private static String repetition(String rle, char delimiter){    String newRLE = &quot;&quot;;    int starting = 0;    while (starting < rle.length())    {        char character = rle.charAt(starting);        if (character == delimiter)        {            starting++;            int sum = getNumber(rle, starting);            char symbol = rle.charAt(starting + String.valueOf(sum).length());            int count = 0;            while (count < sum)             {                newRLE += symbol;                count++;            }            starting += String.valueOf(sum).length() + 1;        }        else         {            newRLE += character;             starting++;        }    }    return newRLE;}