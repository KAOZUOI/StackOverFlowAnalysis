public static void main(String[] args) {    String rle = &quot;^5!^4@^8$&quot;;    char delimiter = '^';    String newRLE = &quot;&quot;;    int starting = 0;    while (starting < rle.length()) {        char character = rle.charAt(starting);        if (character == delimiter) {            starting++;            int number = getNumber(rle, starting);            if(number ==0 ) return;            starting++;            char symbol = rle.charAt(starting);            int count = 0;            while (count < number) {                newRLE += symbol;                count++;            }            starting ++;        } else {            newRLE += character;            starting++;        }    }    System.out.println(newRLE);}private static int getNumber(String rle, int initial) {    if (Character.isDigit(rle.charAt(initial))) {        return Integer.parseInt(String.valueOf(rle.charAt(initial)));    }    return 0;}}