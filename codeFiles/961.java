ServerSocket serverSocket = new ServerSocket(4999);Socket clientSocket = serverSocket.accept();        OutputStream outStream = clientSocket.getOutputStream();PrintWriter outWriter = new PrintWriter(outStream, true);        byte buf[] = new byte[2000]; // buffer size of 2000 as an example        System.out.println(&quot;sending: &quot; + buf.length + &quot; bytes&quot;);outWriter.println(buf.length);// If I put a delay here, for example 1000 System.out.println(1) statements, the client receives the buffer correctly, if not the client receives 0 bytesoutStream.write(buf);clientSocket.close();serverSocket.close();